# Matryoshka Crates

The workspace crates live here, but the main product docs are now at the repo
root:

- `README.md`: what Matryoshka is
- `usage.md`: commands, flags, and workflow examples

The crate-local fixture used by Rust integration tests lives at:

- `crates/indexer/tests/fixtures/mini_repo`
